-
    prefix: '@axios_async'
    body: |-
        async function getUser() {
            try {
                const response = await axios.get('/user?ID=12345');
                console.log(response);
            } catch (error) {
                console.error(error);
            }
        }
    description: es7异步写法
