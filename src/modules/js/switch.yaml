prefix: "@js_switch"
description: "switch"
body: |
    switch(n) {
        case 1:
            // code content
            break;
        case 2:
            // code content
            break;
        default:
            // last code
    }